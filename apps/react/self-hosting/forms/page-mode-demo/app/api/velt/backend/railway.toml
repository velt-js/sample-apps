[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn velt_test_project.wsgi --bind 0.0.0.0:$PORT --log-file -"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
